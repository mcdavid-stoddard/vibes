mkdir silicon-deposit-bot
cd silicon-deposit-bot
# Create the two files above (index.js and package.json)
git init
git add .
git commit -m "initial commit"
git branch -M main
git remote add origin git@github.com:YOUR_USERNAME/silicon-deposit-bot.git
git push -u origin main
